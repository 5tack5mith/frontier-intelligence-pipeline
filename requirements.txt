# Core async + HTTP
httpx>=0.27.0
tenacity>=8.5.0

# Data validation
pydantic>=2.5.0

# Entity resolution
rapidfuzz>=3.9.0

# Date handling
dateparser>=1.2.0

# Feed / HTML parsing
feedparser>=6.0.11
trafilatura>=1.12.0
beautifulsoup4>=4.12.0

# Optional: real tokenizer for the chunker (falls back to word-count
# heuristic automatically if unavailable or if network egress to
# openaipublic.blob.core.windows.net is blocked)
tiktoken>=0.7.0

# Optional: only needed if using Playwright for the one anti-bot demo source
playwright>=1.45.0

# Output
gspread>=6.1.0
google-auth>=2.32.0
pandas>=2.2.0

# Env loading
python-dotenv>=1.0.0
